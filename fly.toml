app = 'faturabot'
primary_region = 'ams'

[build]

[http_service]
  internal_port = 8080
  force_https = false
  auto_stop_machines = false
  auto_start_machines = false

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
