app = 'faturabot'
primary_region = 'ams'

[build]

[processes]
  app = "python bot.py"

[[services]]
  auto_stop_machines = false
  auto_start_machines = false

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
